# PostgreSQL Configuration

**Status:** Default installation, no custom tuning applied yet.

هیچ تنظیم اختصاصی (postgresql.conf، pg_hba.conf) هنوز تغییر نکرده — فقط نصب پیش‌فرض سیستم است.
وقتی Tuning واقعی (Performance، Connection Limits) انجام شد، فایل‌های واقعی اینجا اضافه می‌شوند.
